{
  "//": "ServerUrl is the base URL of the Oceana server; the agent appends /hubs/agents-control. A server is required. Channel-to-device routing is managed from the server, not here.",
  "ServerUrl": "http://localhost:5069"
}
